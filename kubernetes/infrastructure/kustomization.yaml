apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - sources
  # Core infrastructure - order matters
  - sealed-secrets
  - cert-manager
  - local-path
  # Monitoring
  - monitoring
  # Networking is managed separately (installed during bootstrap)
  # - cilium
  # Bare metal provisioning
  # - tinkerbell
  # - tailscale
